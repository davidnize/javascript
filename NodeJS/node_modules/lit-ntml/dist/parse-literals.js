import { processLiterals } from './process-literals';
export function parseLiterals(serializeFn) {
    return async (fn, strings, ...exps) => {
        const content = await processLiterals(strings, ...exps);
        return serializeFn(fn(content));
    };
}
//# sourceMappingURL=parse-literals.js.map