import { processLiteralsSync } from './process-literals-sync';
export function parseLiteralsSync(serializeFn) {
    return (fn, strings, ...exps) => {
        const content = processLiteralsSync(strings, ...exps);
        return serializeFn(fn(content));
    };
}
//# sourceMappingURL=parse-literals-sync.js.map